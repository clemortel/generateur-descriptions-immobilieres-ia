# Static serving — bypass Nuxt build for Stitch landing page
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm i -g serve"]

[phases.build]
cmds = []

[start]
cmd = "serve public -l 3000"
